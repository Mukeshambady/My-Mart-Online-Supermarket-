<link rel="stylesheet" href="/css/cropper.css">
<style>
	.label {
		cursor: pointer;
	}

	.progress {
		display: none;
		margin-bottom: 1rem;
	}

	.alert {
		display: none;
	}

	.img-container img {
		/* max-width: 100%; */
		width: 100%;
		height: 100%;
	}
</style>
<div class="container-fluid">
    <!-- Section: Edit Account -->

    <section class="section">
        {{#if productSuccess}}
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            <strong id="registrationStatus">{{productSuccess}}</strong>
            <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                <span aria-hidden="true">&times;</span>
            </button>
        </div>
        {{/if}}
        {{#with productDetails}}
        <form class="form-horizontal" method="POST" id="product-form" action="/dealer/edit-product/{{_id}}"
            enctype="multipart/form-data">
            <!-- First row -->
            <div class="row">
                {{!-- Form start --}}

                <!-- First column -->
                <div class="col-lg-4 mb-4">

                    <!-- Card -->
                    <div class="card card-cascade narrower">

                        <!-- Card image -->
                        <div class="view view-cascade gradient-card-header mdb-color lighten-3">
                            <h5 class="mb-0 font-weight-bold">Product</h5>
                        </div>
                        <!-- Card image -->

                        <!-- Card content -->
                        <div class="card-body card-body-cascade text-center">
                            <img src="{{productImage}}" alt="User Photo"
                                class="z-depth-1 mb-3 mx-auto img-fluid" alt="User Photo" id="imageView" name="image1" onclick='$("#FileUpload").click()'>
                           
                            <p id="img-name" class="text-muted "><small>Click to change your product
                                    image</small></p>
                            <div class="row flex-center">
                               <input type="file" id="FileUpload" style="display: none;">
								<input type="hidden" name="image" id="newimage">
                                <a class="btn btn-info btn-rounded btn-sm waves-effect waves-light"
                                    onclick='$("#FileUpload").click()'>Upload New
                                    Product</a>
                              
                            </div>
                        </div>
                        <!-- Card content -->

                    </div>
                    <!-- Card -->

                </div>

                <!-- First column -->

                <!-- Second column -->
                <div class="col-lg-8 mb-4">

                    <!-- Card -->
                    <div class="card card-cascade narrower">

                        <!-- Card image -->
                        <div class="view view-cascade gradient-card-header mdb-color lighten-3">
                            <h5 class="mb-0 font-weight-bold">Update Product Details</h5>
                        </div>
                        <!-- Card image -->

                        <!-- Card content -->
                        <div class="card-body card-body-cascade text-center">
                            <!-- First row -->
                            <div class="row">
                                <!-- First column -->
                                <div class="col-md-12">
                                    <div class="md-form mb-0">
                                        <input type="text" class="form-control validate" value="{{name}}" name="name"
                                            required placeholder="Product Name">
                                        <label for="form1" data-error="wrong" data-success="right">Product Name</label>
                                    </div>
                                    {{#if productNameExistError}}
                                    <div class="alert alert-danger  alert-dismissible fade show" role="alert">
                                        <strong>{{productNameExistError}}</strong>
                                    </div>

                                    {{/if}}
                                </div>
                            </div>
                            <div class="row">
                                <!-- Second column -->
                                <div class="col-md-12">
                                    <div class="md-form mb-0">
                                        <input type="text" name="category" class="form-control validate" required
                                            placeholder="Product Category" value="{{category}}">
                                        <label for="product category" data-error="wrong" data-success="right">Product
                                            Category</label>
                                    </div>
                                </div>
                            </div>
                            <!-- First row -->


                            <!-- First row -->
                            <div class="row">
                                <!-- First column -->
                                <div class="col-md-3">
                                    <div class="md-form mb-0">
                                        <input type="number" id="form81" name="price" placeholder="Product Price"
                                            class="form-control validate" required value="{{price}}">
                                        <label for="Product Price" data-error="wrong" data-success="right">Product
                                            Price</label>
                                    </div>
                                </div>
                                 <!-- Secound column -->
                                <div class="col-md-3">
                                    <div class="md-form mb-0">
                                        <input type="number" id="form81" name="weight" placeholder="Product Weight"
                                            class="form-control validate" required value="{{weight}}">
                                        <label for="Product Weight" data-error="wrong" data-success="right">Product
                                            Weight</label>

                                    </div>
                                </div>
                                <!-- Thired column -->
                                <div class="col-md-3">
                                    <div class="md-form mb-0">
                                        <select name="measure" class="mdb-select form-control validate" required>
                                            <option value="">Select One Measure</option>
                                            <option value="Kg" {{#if (eqIf measure 'Kg' ) }}selected{{/if}}>Kg</option>
                                            <option value="g" {{#if (eqIf measure 'g' ) }}selected{{/if}}>g</option>
                                        </select>

                                        <label for="Unit" data-error="wrong" data-success="right"></label>

                                    </div>
                                </div>
                                 <div class="col-md-3">
                                    <div class="md-form mb-0">
                                        <input type="number" id="form81" name="stock" value="{{stock}}"
                                            placeholder="number of qty" class="form-control validate" required>
                                        <label for="Product stock" data-error="wrong" data-success="right">Product
                                            Stock</label>
                                    </div>
                                </div>

                            </div>
                            <!-- First row -->

                            <!-- First row -->
                            <div class="row">
                                <!-- First column -->
                                <div class="col-md-12">
                                    <div class="md-form mb-0">
                                        <textarea type="text" name="productInfo" class="md-textarea form-control"
                                            rows="3">{{productInfo}}</textarea>
                                        <label for="form78">Product Info</label>
                                    </div>
                                </div>

                            </div>
                            <!-- First row -->
                            <!-- Fourth row -->
                            <div class="row">
                                <div class="col-md-12 text-center my-1">

                                    <input type="submit" value="Update"
                                        class="btn btn-outline-info btn-rounded btn-sm btn-ok">

                                </div>
                            </div>
                            <!-- Fourth row -->



                        </div>
                        <!-- Card content -->

                    </div>
                    <!-- Card -->

                </div>
                <!-- Second column -->

            </div>
            <!-- First row -->
        </form>
    </section>
    {{/with}}
    <!-- Section: add Account -->


</div>




{{!-- Modal Image crop --}}
<div class="modal fade " id="modal" data-backdrop="static" tabindex="-1" role="dialog" aria-labelledby="modalLabel"
	aria-hidden="true">
	<div class="modal-dialog" role="document">
		<div class="modal-content modal-lg">
			<div class="modal-header">
				<h5 class="modal-title" id="modalLabel">Crop the image</h5>
				<button type="button" class="close" data-dismiss="modal" aria-label="Close">
					<span aria-hidden="true">&times;</span>
				</button>
			</div>
			<div class="modal-body">
				<div class="img-container">
					<img id="image" src="https://avatars0.githubusercontent.com/u/3456749">
				</div>
			</div>
			<div class="modal-footer">
				<button type="button" class="btn btn-secondary" data-dismiss="modal">Cancel</button>
				<button type="button" class="btn btn-primary" id="crop">Crop</button>
			</div>
		</div>
	</div>
</div>
{{!-- Modal Image crop --}}

<script src="/js/cropper.js"></script>
<script>
	window.addEventListener('DOMContentLoaded', function () {

		var avatar = document.getElementById('imageView');
		var image = document.getElementById('image');
		var input = document.getElementById('FileUpload');

		var $modal = $('#modal');
		var cropper;

		$('[data-toggle="tooltip"]').tooltip();

		input.addEventListener('change', function (e) {
			var files = e.target.files;
			var done = function (url) {
				input.value = '';
				image.src = url;

				$modal.modal('show');
			};
			var reader;
			var file;
			var url;

			if (files && files.length > 0) {
				file = files[0];

				if (URL) {
					done(URL.createObjectURL(file));
				} else if (FileReader) {
					reader = new FileReader();
					reader.onload = function (e) {
						done(reader.result);
					};

					reader.readAsDataURL(file);
				}
			}
		});

		$modal.on('shown.bs.modal', function () {
			cropper = new Cropper(image, {
				aspectRatio: 1,
				viewMode: 3,
			});
		}).on('hidden.bs.modal', function () {
			cropper.destroy();
			cropper = null;
		});

		document.getElementById('crop').addEventListener('click', function () {
			var initialAvatarURL;
			var canvas;

			$modal.modal('hide');

			if (cropper) {
				canvas = cropper.getCroppedCanvas({
					width: 400,
					height: 400,
				});
				initialAvatarURL = avatar.src;
				avatar.src = canvas.toDataURL();
				$('#newimage').val(canvas.toDataURL())
			}
		});
	});
</script>